// CustomHistory1---------------------------------------------------------------
// Here we will create our custom History object
// Using this we can change easily
// The history package is automatically installed with react router dom
    //--------------------------------------------------------------------------
    // This type of initialization will be removed in next React update
    // import createHistory from 'history/createBrowserHistory';
    // export default createHistory();
    //--------------------------------------------------------------------------
import { createBrowserHistory } from "history";

export default createBrowserHistory();
//------------------------------------------------------------------------------
